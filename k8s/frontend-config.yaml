apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: task-api
data:
  # Backend URL for nginx proxy in frontend container
  BACKEND_URL: "backend-service.task-api.svc.cluster.local:8080"
