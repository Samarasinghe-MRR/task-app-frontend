# Environment configuration for Docker Compose (VM deployment)
# Use relative path - nginx proxy will handle routing to backend
VITE_API_BASE_URL=/api

# For direct external access (alternative):
# VITE_API_BASE_URL=http://34.100.195.80/api